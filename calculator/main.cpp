#include <iostream>
#include <vector>
#include <string>
#include <ctype.h>

/*
class Token {
    public:
        int value;
*/

double calculate(std::string input) {
    double value = 0
    for (char & c : input){
        double token = 0;
        if (isdigit(c)):
            token = 

double calculate(std::string input) {
    

int main() {
    std::string input;
    getline(std::cin, input);

    double answer;
    answer = calculate(input);
    
